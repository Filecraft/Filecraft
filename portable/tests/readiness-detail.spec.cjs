const {test,expect}=require('@playwright/test');const path=require('node:path');const {pathToFileURL}=require('node:url');
test('readiness shows measured ranges and dimensional expectations',async({page})=>{await page.goto(pathToFileURL(path.resolve(__dirname,'../index.html')).href);await page.evaluate(()=>ReadinessUI.render(document.getElementById('readiness'),{status:'NOT_READY',profileId:'custom',checks:[{code:'BYTES',state:'fail',params:{actual:50,min:100,max:200},remediation:['ADJUST_FILE_SIZE']},{code:'PAGE_DIMENSIONS',state:'fail',params:{pageId:'one',width:300,height:400,unit:'pt',expected:{unit:'pt',minWidth:500,maxWidth:600,minHeight:700,maxHeight:800}},remediation:['RESIZE_OR_VERIFY_PAGE']},{code:'PAGE_ORIENTATION',state:'fail',params:{pageId:'one',width:300,height:400,unit:'pt',expected:'landscape'},remediation:['ROTATE_OR_VERIFY_PAGE']},{code:'FILENAME',state:'fail',params:{actual:'long.pdf',maxLength:4,asciiOnly:true,extensions:['pdf']},remediation:['RENAME_FILE']}]}));await expect(page.locator('#readiness')).toContainText('min 100');await expect(page.locator('#readiness')).toContainText('300 × 400 pt');await expect(page.locator('#readiness')).toContainText('width 500–600');await expect(page.locator('#readiness')).toContainText('expected landscape');await expect(page.locator('#readiness')).toContainText('max 4 characters')});
