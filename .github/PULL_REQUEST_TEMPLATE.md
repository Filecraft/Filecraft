## What and why

Closes #

## Verification

- [ ] Added a regression check that failed before the fix (for behavior changes).
- [ ] `swift run PrepareChecks` and `swift run -c release PrepareChecks` pass.
- [ ] `swift build -c release -Xswiftc -warnings-as-errors` passes.
- [ ] Inspected exported PDF pixels, page count and byte limit where relevant.
- [ ] Originals, cancellation, stale-preview invalidation and offline operation remain intact.
- [ ] Updated docs and CHANGELOG; disclosed AI assistance if used.
- [ ] I have the right to contribute this work under Apache-2.0 (with upstream exceptions preserved).

## Evidence

Include commands, results, OS/Swift version and synthetic screenshots where useful. Never attach private documents.
